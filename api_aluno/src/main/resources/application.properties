spring.datasource.url=jdbc:mysql://localhost:3306/banco
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update


#executar no cmd como administrador:

#pra finalizar um proceso q está ocupando a porta 3306:
#netstat -ano | findstr :3306

#Você já identificou que o PID (Process Identifier) é 6216, que está usando a porta 3306.
#No terminal, execute o seguinte comando para finalizar o processo:
#taskkill /PID 6216 /F